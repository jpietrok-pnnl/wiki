# build time tests for table plugin
# see http://mochajs.org/

table = require '../client/table'
expect = require 'expect.js'

describe 'table plugin', ->

  describe 'expand', ->

    it 'can make itallic', ->
      result = table.expand 'hello *world*'
      expect(result).to.be 'hello <i>world</i>'
