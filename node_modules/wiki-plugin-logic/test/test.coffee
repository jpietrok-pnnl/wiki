# build time tests for logic plugin
# see http://mochajs.org/

logic = require '../client/logic'
expect = require 'expect.js'

describe 'logic plugin', ->

  describe 'expand', ->

    it 'can make itallic', ->
      result = logic.expand 'hello *world*'
      expect(result).to.be 'hello <i>world</i>'
