# build time tests for statement plugin
# see http://mochajs.org/

statement = require '../client/statement'
expect = require 'expect.js'

describe 'statement plugin', ->

  describe 'expand', ->

    it 'can make itallic', ->
      result = statement.expand 'hello *world*'
      expect(result).to.be 'hello <i>world</i>'
